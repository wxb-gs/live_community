server:
  port: 8080

spring:
  application:
    name: gateway

dubbo:
  application:
    name: gateway
  registry:
    address: nacos://localhost:8848

jwt:
  secret: super-secret-key-change-in-production
